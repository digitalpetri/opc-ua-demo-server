# Populated by Maven resource filtering. The values come from buildnumber-maven-plugin (validate
# phase) and the project version. Read at runtime by BuildProperties.
software.version=${project.version}
build.number=${buildNumber}
build.date=${timestamp}
